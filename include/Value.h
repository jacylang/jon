#ifndef JON_VALUE_H
#define JON_VALUE_H

#include <variant>
#include <memory>

namespace jon {
    struct Value;
    using value_ptr = std::unique_ptr<Value>;

    enum class Type {

    };

    struct Value {

    };
}

#endif // JON_VALUE_H
