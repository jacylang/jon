#ifndef JON_VALUE_H
#define JON_VALUE_H

#include <variant>
#include <memory>

namespace jon {
    struct NValue;
    using n_value_ptr = std::unique_ptr<NValue>;

    enum class Type {

    };

    struct NValue {

    };
}

#endif // JON_VALUE_H
